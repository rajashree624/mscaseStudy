package com.ibm.casestudy.accountms.controller;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.ResponseBody;


@Controller  
public class WebController {
	/*
	 * @RequestMapping("/")
	 * 
	 * @ResponseBody public String index() { return "You made it!"; } // Login form
	 * 
	 * @RequestMapping("/login.html") public String login() { return "login.jsp"; }
	 * // Login form with error
	 * 
	 * @RequestMapping("/login-error.html") public String loginError(Model model) {
	 * model.addAttribute("loginError", true); return "login.jsp"; }
	 * 
	 * // Login form with error
	 * 
	 * @RequestMapping("/loginSuccess") public String
	 * loginSuccess(HttpServletRequest request,HttpServletResponse response,Model
	 * model) { System.out.println("inside login success"); return "login.html"; }
	 */}
